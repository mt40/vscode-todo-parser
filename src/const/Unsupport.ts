/**
 * Normally, languages that are not listed in LanguageType will 
 * be treated as plain text. However, in some cases, we don't 
 * even want to process a certain language at all. Such language
 * should be added here
 * Note that for convenience, add the file extension of that
 * language instead
 */
export const UnsupportFiles = ["json"];