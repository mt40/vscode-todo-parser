// Todo: typescript one line Todo
let x = 0;
let str = "superman";


/**
 * 
 * @export
 * TODO: typescript multiline
 * todo
 * 
 * @param {string} str
 * @returns {*}
 */
export function doSomething(str: string): any {
  return str + " " + str;
}